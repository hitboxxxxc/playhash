// Re-export do countdown COMPARTILHADO (movido para `core/widgets` para
// reuso na HOME — "Próxima recompensa em" usa o MESMO ticker da MINERAÇÃO).
// Import existente em `features/mining/widgets/next_block_countdown.dart`
// continua válido.
export '../../../core/widgets/next_block_countdown.dart';
